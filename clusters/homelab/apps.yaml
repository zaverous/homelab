# Tells Flux to also reconcile ./apps/hello-ci, separate from the
# flux-system bootstrap files that live alongside this in clusters/homelab/.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./apps/hello-ci
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
